<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    
    
    
    
    
    <Style x:Key="MenuBar" TargetType="ListBoxItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        x:Name="El_bg"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Margin="0,0,0,0"
                        Background="Transparent"
                        CornerRadius="5">

                        <!--<i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseLeftButtonDown">
                                <i:InvokeCommandAction
                                Command="{Binding DataContext.NavigateCommand,
                                                  RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding Name, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>-->

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <Border
                                x:Name="Indicator"
                                Grid.Column="0"
                                Width="5"
                                Height="20"
                                Margin="0,0,5,0"
                                VerticalAlignment="Stretch"
                                Background="AliceBlue"
                                CornerRadius="2"
                                Visibility="Hidden" />

                            <ContentPresenter
                                x:Name="icon"
                                Grid.Column="1"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Button FontFamily="../Font/#iconfont" Content="{TemplateBinding Content}" />
                                            <!--<TextBlock Text="{TemplateBinding}"/>-->
                                        </Border>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="El_bg" Property="Background" Value="{StaticResource PressedBackground}" />
                            <Setter TargetName="Indicator" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="El_bg" Property="Background" Value="{StaticResource SelectedBackground}" />
                            <!--<Setter TargetName="Indicator" Property="Visibility" Value="Hidden"/>-->
                        </Trigger>
                        <!--  <Trigger Property=""  -->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
</ResourceDictionary>