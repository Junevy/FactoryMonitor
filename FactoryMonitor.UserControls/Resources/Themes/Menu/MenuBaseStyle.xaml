<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FactoryMonitor.UserControls.Controls.Menu"
    xmlns:helper="clr-namespace:FactoryMonitor.UserControls.Helper">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <Style x:Key="MenuStyleBase" TargetType="{x:Type controls:MenuBase}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <!--<Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>-->

        <!--  内容  -->
        <Setter Property="Template">
            <Setter.Value>
                <!--  容器  -->
                <ControlTemplate TargetType="{x:Type controls:MenuBase}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!--  布局  -->
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=Orientation}" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=ElementOrientation}">
                        <Button
                            Width="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=MenuItemWidth}"
                            Height="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=MenuItemHeight}"
                            Margin="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=MenuItemMargin}"
                            Command="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=NavigateCommand, Mode=TwoWay}"
                            CommandParameter="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border
                                        x:Name="ContainerBorder"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        Background="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=MenuItemBackground}"
                                        CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=MenuItemCornerRadius}">

                                        <Grid RenderTransformOrigin="0.5,0.5">
                                            <Grid.RenderTransform>
                                                <ScaleTransform x:Name="ButtonScale" ScaleX="1" ScaleY="1" />
                                            </Grid.RenderTransform>

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition MaxWidth="3" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <Border
                                                x:Name="Indicator"
                                                Grid.Column="0"
                                                Width="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=IndicatorWidth}"
                                                Height="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=IndicatorHeight}"
                                                HorizontalAlignment="Left"
                                                Background="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=IndicatorBackground}"
                                                CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=IndicatorCornerRadius}"
                                                Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <ContentPresenter
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Content="{Binding}">
                                                <ContentPresenter.ContentTemplate>
                                                    <DataTemplate>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <StackPanel
                                                                x:Name="OrientationPanel"
                                                                Grid.Column="0"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch"
                                                                Orientation="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=Orientation}">

                                                                <ContentControl
                                                                    HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"
                                                                    Content="{Binding Icon}"
                                                                    FontFamily="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=(helper:IconHelper.IconFontFamily)}"
                                                                    FontSize="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=(helper:IconHelper.IconSize)}"
                                                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=(helper:IconHelper.IconForeground)}" />

                                                                <TextBlock
                                                                    Margin="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=ContentMargin}"
                                                                    HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"
                                                                    FontFamily="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=ContentFontFamily}"
                                                                    FontSize="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=ContentSize}"
                                                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=Foreground}"
                                                                    Text="{Binding Content}"
                                                                    Visibility="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=ContentVisibility}" />
                                                            </StackPanel>

                                                            <Button
                                                                x:Name="CloseButton"
                                                                Grid.Column="1"
                                                                Margin="10,0,10,0"
                                                                HorizontalAlignment="Right"
                                                                VerticalAlignment="Center"
                                                                Command="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=CloseCommand, Mode=TwoWay}"
                                                                CommandParameter="{Binding}"
                                                                Content="&#xe639;"
                                                                FontFamily="{DynamicResource IconFont}"
                                                                Visibility="{Binding RelativeSource={RelativeSource AncestorType=controls:MenuBase}, Path=CloseButtonVisibility}">
                                                                <Button.Template>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border
                                                                            x:Name="CloseButtonBg"
                                                                            Background="{DynamicResource SubBackground}"
                                                                            CornerRadius="5">
                                                                            <Label Content="{TemplateBinding Content}" />
                                                                        </Border>

                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="CloseButtonBg" Property="Background" Value="Gray" />
                                                                            </Trigger>
                                                                            <Trigger Property="IsMouseCaptured" Value="True">
                                                                                <Setter TargetName="CloseButtonBg" Property="Background" Value="Tomato" />
                                                                            </Trigger>

                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Button.Template>
                                                            </Button>
                                                        </Grid>

                                                        <DataTemplate.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=OrientationPanel, Path=Orientation}" Value="Vertical">
                                                                <Setter TargetName="CloseButton" Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </DataTemplate.Triggers>
                                                    </DataTemplate>
                                                </ContentPresenter.ContentTemplate>
                                            </ContentPresenter>
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <!--  点击缩放动画  -->
                                        <Trigger Property="IsPressed" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ButtonScale"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="0.5"
                                                            Duration="0:0:0.2" />
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ButtonScale"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="0.5"
                                                            Duration="0:0:0.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ButtonScale"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="1"
                                                            Duration="0:0:0.2" />
                                                        <DoubleAnimation
                                                            Storyboard.TargetName="ButtonScale"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="1"
                                                            Duration="0:0:0.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>

                                        <!--  选中高亮  -->
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter TargetName="Indicator" Property="Opacity" Value="1" />
                                            <Setter TargetName="ContainerBorder" Property="Background" Value="{DynamicResource PressedBackground}" />
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>